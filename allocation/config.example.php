<?php
/**
 * קובץ קונפיגורציה לדוגמה - מודול הקצאת חשבוניות (רשות המסים)
 * * קובץ זה משמש כתבנית בלבד. יש לשכפל קובץ זה, לקרוא לעותק החדש config.php
 * ולמלא בו את פרטי הגישה האמיתיים והסודיים של החברה.
 */

return [
    // 1. סביבת העבודה הנוכחית של המערכת
    // ערכים אפשריים: 'sandbox' (סביבת בדיקות) או 'production' (סביבת אמת/ייצור)
    'environment' => 'sandbox', [cite: 59]

    // 2. הגדרות אימות ואבטחה מול שרת ה-OAuth2 של רשות המסים
    'oauth' => [
        // כתובת ה-URL של שרת ההזדהות לקבלת אסימון הגישה (Access Token)
        'token_url'     => 'https://ita-api.taxes.gov.il/shaam/tsandbox/oauth2/token', [cite: 60]
        
        // מזהה הלקוח הייחודי שניתן לחברה ע"י רשות המסים (ציבורי אך ייחודי)
        'client_id'     => 'YOUR_CLIENT_ID', [cite: 60]
        
        // המפתח הסודי והרגיש ביותר של החברה. משמש לחתימת הבקשות (חובה לשמור בסוד!)
        'client_secret' => 'YOUR_CLIENT_SECRET', [cite: 60]
        
        // הרשאות הגישה הנדרשות מה-API (במקרה זה, גישה למודול אישור חשבוניות)
        'scope'         => 'Invoices', [cite: 60]
    ],

    // 3. נקודות הקצה (Endpoints) הרשמיות של שירות ה-Approval של רשות המסים
    'api' => [
        // כתובת ה-API הסטנדרטית לבדיקות ופיתוח (סביבת ארגז חול)
        'approval_url_sandbox'    => 'https://ita-api.taxes.gov.il/shaam/tsandbox/Invoices/v2/Approval', 
        
        // כתובת ה-API הרשמית לחשבוניות אמת מול שע"מ
        'approval_url_production' => 'https://ita-api.taxes.gov.il/shaam/production/Invoices/v2/Approval', 
    ], 

    // 4. נתוני הזיהוי הרשמיים של החברה שלנו (המנפיקה את החשבונית)
    'company' => [
        // מספר ח.פ או עוסק מורשה של החברה המפעילה את ה-CRM
        'vat_number'                 => '56858499', 
        // מספר רישום רשמי של תוכנת ה-CRM ברשות המסים (מזהה את בית התוכנה)
        'accounting_software_number' => 'XXXXXXXXX', 
    ],

    // 5. ספי הסכומים (לפני מע"מ) המחייבים קבלת מספר הקצאה על פי חוק
    // המערכת בודקת דינמית את תאריך החשבונית וקובעת את הסף בהתאם לציר הזמן
    'thresholds' => [
        '2026-01-01' => 10000, // החל מינואר 2026: חובה מעל 10,000 ש"ח [cite: 14, 61]
        '2026-06-01' => 5000,  // החל מיוני 2026: הסף יורד ומחייב כל חשבונית מעל 5,000 ש"ח [cite: 14, 61]
    ],

    // 6. הגדרות התנהגות פנימיות של מודול ההקצאה ב-CRM
    'allocation' => [
        // האם המודול פעיל? (מאפשר כיבוי חירום ז[cite: 61, 62]
        
        // במידה ומוגדר כ-true, המערכת תבקש הקצאה לכל חשבונית, ללא קשר לסכום או לסוג הלקוח
        'request_for_all_invoices' => false,
        
        // האם לשמור במסד הנתונים את המספר הארוך והמלא שהתקבל (למשל 26 ספרות)
        'store_full_number'        => true, 
        
        // מספר הספרות הימניות שיש לחתוך מתוך המספר המלא לצורך הדפסה על ה-PDF ושליחה ל-Priority
        'display_digits'           => 9, 
    ],

    // 7. הגדרות תאימות מול קובץ הממשק של מערכת ה-Priority
    'priority' => [
        // האינדקס (המיקום המבוסס 0) של שדה "אסמכתא 2" בתוך שורת הטקסט המופרדת בטאבים
        'asmachta2_field_index' => 8, 
    ],

    // 8. הגדרות מערכת רישום הלוגים (Logging) של ה-API
    'logging' => [
        // הנתיב המוחלט או היחסי שבו יישמר קובץ הלוג של תהליך ההקצאה
        'file_path'           => __DIR__ . '/log/allocation.log', 
        
        // מזהה תהליך פנימי (Process ID) המשמש לסינון וזיהוי הלוגים של מודול זה במערכת ה-API_LOG הכללית
        'api_log_process_id'  => 33, 
    ],
];